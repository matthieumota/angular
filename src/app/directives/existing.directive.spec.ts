import { ExistingDirective } from './existing.directive';

describe('ExistingDirective', () => {
  it('should create an instance', () => {
    const directive = new ExistingDirective();
    expect(directive).toBeTruthy();
  });
});
